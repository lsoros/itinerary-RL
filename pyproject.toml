[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "itinerary-rl"
version = "0.1.0"
description = "OpenEnv flight itinerary environment"
requires-python = ">=3.12"
dependencies = ["PyYAML>=6"]

[tool.setuptools.packages.find]
where = ["src"]
